<%= form_for(@captcha) do |f| %>

  <div class="form-inputs">
    <%= f.text_field :name %>
  </div>

	<div class="form-inputs">
	  <%= f.file_field :photo %>
  </div>  

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
