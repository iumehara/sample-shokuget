<% flash.each do |name, msg| %>
  <div class=" container alert alert-<%= name == :notice ? "success" : "danger" %>">
    <a class="close" data-dismiss="alert">×</a>  	    
    <%= msg %>
  </div>
<% end %>
